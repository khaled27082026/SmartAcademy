# CLAUDE.md — قواعد التصميم والبناء

> ملف توجيه لـ Claude Code داخل VS Code. مبني على تحليل **الكود الفعلي (CSS)**
> لموقع مرجعي (منصة تعليمية عربية). كل القيم هنا **حقيقية ومؤكدة من الكود**، مش
> افتراضات. الهدف: بناء موقع بنفس روح التصميم (Dark Glassmorphism + Neon
> Gradients) بكود جديد بالكامل يخصك انت.

---

## 1. نظرة عامة على الطابع البصري

تصميم من فئة **"Dark Glass / Neon EdTech"**:
- خلفية داكنة جدًا (Navy/Slate داكن) بدل الأبيض.
- بطاقات شبه شفافة (Glassmorphism) بحدود خفيفة مضيئة.
- تدرجات لونية (Gradients) نيون بين البنفسجي والوردي والسماوي تُستخدم في
  الأزرار، العناوين، والأيقونات.
- حركة وتفاعل ناعم في كل مكان (hover transforms, glow shadows, blur pulse).
- اتجاه الصفحة **RTL بالكامل** (عربي).

---

## 1.1 الإطار التقني الأساسي (Tech Stack)

الموقع مبني فوق **Bootstrap v5.1.0** كإطار أساسي (grid system, containers,
utility classes زي `d-flex`, `gap-*`, `rounded-*`, إلخ)، وبعدين الـ CSS
المخصص (Custom CSS) بتاعه بيـ**تجاوز (override)** متغيرات Bootstrap الافتراضية
بالكامل. يعني لو هتستخدم Bootstrap في مشروعك، اعرف إن:

- ألوان Bootstrap الافتراضية (`--bs-primary: #0d6efd` الأزرق، إلخ) **مش
  مستخدمة فعليًا** في التصميم — كلها متجاوزة بمتغيرات مخصصة (قسم 2 تحت).
- استخدم Bootstrap فقط للـ **layout والـ utilities** (`.container`, `.row`,
  `.col-*`, `.d-flex`, `.gap-*`, `.rounded-*`) — ومكونات زي `.btn`, `.card`,
  `.dropdown-menu`، `.navbar` بس بعد ما تدّيها ستايل مخصص بالكامل بالمتغيرات
  والكلاسات المخصصة اللي هتلاقيها في القسم اللي جاي.
- لا تعتمد على ألوان أو أحجام Bootstrap الجاهزة في أي مكان — كل حاجة بصرية
  لازم تتحدد بمتغيرات `:root` المخصصة.

---

## 2. نظام الألوان (Design Tokens)

> ⚠️ **تحديث مهم**: شعار مشروعك الفعلي (**Smart Academy**) بألوانه الحقيقية
> (مستخرجة بتحليل بكسل مباشر من ملف الشعار) هو **أزرق بحري/بترولي احترافي**،
> مختلف تمامًا عن ألوان الموقع المرجعي (نيون بنفسجي/وردي). القرار هنا: نبني
> بنفس **أسلوب** الموقع المرجعي (Dark Glassmorphism + Glow) لكن بلوحة ألوان
> **متجانسة مع هوية شعارك انت**، مش الألوان الجاهزة من موقع حد تاني.

### ألوان الشعار (مؤكدة من تحليل بكسل مباشر)
```css
--logo-primary: #0a425b;   /* أزرق بحري داكن - اللون المسيطر في الشعار */
--logo-secondary: #2f5f75; /* أزرق/تركواز أفتح - الوجه الجانبي للمكعب وكلمة ACADEMY */
```

### نظام الألوان الكامل المقترح للموقع (مبني على ألوان الشعار)
```css
:root {
    --bg-core: #071a26;       /* خلفية أساسية داكنة جدًا، من نفس عائلة الشعار */
    --bg-card: #0f2c3d;       /* خلفية البطاقات - أفتح خطوة من الخلفية */
    --primary: #0a425b;       /* أزرق بحري - نفس لون الشعار الأساسي بالظبط */
    --accent: #2f5f75;        /* الأزرق/التركواز الفاتح من الشعار - لون التمييز */
    --cyan: #4fb3d9;          /* سماوي أفتح مشتق من نفس العائلة - للـ hover والتفاعل */
    --text-main: #f1f5f9;     /* نص أساسي فاتح */
    --text-sec: #94a3b8;      /* نص ثانوي رمادي مائل للأزرق */
    --glass: rgba(255, 255, 255, 0.03);
    --border-glass: rgba(255, 255, 255, 0.08);
}
```

**ملاحظة على `--cyan`**: مش موجود في الشعار بشكل مباشر، لكنه لون سماوي مشتق
من نفس درجة الأزرق (Hue) بتاعة الشعار لكن بإضاءة (Lightness) أعلى، عشان يفضل
في نفس العائلة اللونية ويشتغل كلون تفاعلي واضح فوق الخلفية الداكنة. لو حابب
تتأكد بعينك، افتح أداة زي [coolors.co](https://coolors.co) وحط `#0a425b` و
`#2f5f75` وشوف التدرجات المقترحة.

قيم إضافية للحالات الخاصة (زي الموقع المرجعي، مش من الشعار):
```css
--danger-gradient-start: #d43f27;  /* زر تسجيل الخروج */
--danger-gradient-end: #d23957;
--logout-red: #ff6b6b;
--logout-red-hover: #ff8787;
--track-bg: #334155;
```

**قاعدة استخدام الألوان**:
- `--primary` → `--accent` (تدرج 90deg) = لون الأزرار الأساسية (`btn-glow`) —
  نفس تدرج ألوان شعارك بالظبط.
- `--cyan` → `--primary` (تدرج 45deg) = لون الأيقونات المميزة (`f-icon`، نص بتدرج).
- `--accent` وحده = عناوين فرعية صغيرة، اسم المدرّس تحت عنوان الكورس، فواصل
  الاقتباس في الـ Hero.
- `--cyan` وحده = hover states، الروابط، التاجات (tags).

---

## 3. الخط (Typography) — مؤكد

```css
* {
    font-family: 'Tajawal', sans-serif;
}
```

- الخط المستخدم فعليًا هو **Tajawal** (خط عربي/لاتيني من Google Fonts) على كل
  عناصر الصفحة بلا استثناء.
- استدعيه في المشروع كده:

```html
<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;900&display=swap" rel="stylesheet">
```

أحجام وأوزان مستخدمة فعليًا:
- `h1` (Hero): `font-size: 4rem; font-weight: 900;` مع `line-height: 1.1`
- عناوين الأقسام (`section-head h2`): `font-size: 2.5rem`
- أرقام الإحصائيات (`stat-num`): `font-size: 3rem; font-weight: 900;`
- الشعار (`logo`): `font-size: 1.8rem; font-weight: 900;`
- نص الوصف (`hero-text p`): `font-size: 1.2rem; line-height: 1.8;`

---

## 4. الأيقونات

الكود يستخدم عنصر `<i>` بأسلوب مكتبات الأيقونات القياسية (نفس نمط
**Font Awesome**). كل مكان فيه أيقونة بيتحكم في حجمها ولونها عبر CSS مباشر
(`font-size`, `color`) بدون SVG مخصص. لازم تستورد المكتبة في `<head>`:

```html
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
```

أماكن استخدام الأيقونات: قوائم البروفايل (`dropdown-item i`)، بطاقات المراحل
(`stage-img-wrapper i` بحجم `5rem` شفاف يتلون عند hover)، المواد الدراسية
(`subject-pill i`)، روابط الموبايل (`mobile-nav-link i`)، أيقونة الـ CTA
الكبيرة (`cta-icon` بحجم `8rem`).

---

## 5. المكونات الأساسية (Components) — من الكود الفعلي

### الأزرار
```css
.btn-glow {
    padding: 10px 25px;
    background: linear-gradient(90deg, var(--primary), var(--accent));
    border-radius: 50px;              /* دائري بالكامل */
    box-shadow: 0 0 15px rgba(10, 66, 91, 0.5);  /* توهج (glow) بلون primary الجديد */
}
.btn-glow:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 30px rgba(10, 66, 91, 0.7);  /* توهج أقوى عند hover */
}
/* ملاحظة: القيم rgba أعلاه محسوبة من --primary الجديد (#0a425b = 10,66,91)
   بدل rgba(99,102,241) الأصلية اللي كانت بتطابق البنفسجي القديم */
```
- زر ثانوي (`btn-outline-glow`): شفاف بحدود بيضاء خافتة، يتحول للون `--cyan`
  عند hover.
- زر تسجيل الخروج (`.logout`): نفس الشكل لكن بتدرج أحمر (`#d43f27` → `#d23957`).

### البطاقات الزجاجية (Glass Cards)
```css
.feature-box {
    background: var(--glass);
    border: 1px solid var(--border-glass);
    border-radius: 20px;
}
.feature-box:hover {
    transform: translateY(-10px);
    background: var(--bg-card);   /* تتحول لخلفية صلبة عند hover */
}
```
مع خط علوي متحرك (`::before` بارتفاع 5px، تدرج primary→cyan، يظهر بـ
`scaleX` من اليمين عند hover) — تفصيلة مميزة تستحق تكرارها في بطاقات أخرى.

### بطاقة الكورس (`course-card-dark`)
- خلفية `--bg-card`، `border-radius: 20px`، صورة بارتفاع ثابت `200px`
  (`object-fit: cover`)، عند hover: `translateY(-8px)` + حدود بلون `--primary`
  + ظل قوي.

### بطاقة المدرّس (`teacher-box`)
- صورة دائرية `160x135px` بحدود `3px solid var(--primary)` وظل بنفسجي خفيف،
  تكبير بسيط `scale(1.05)` عند hover.

### بطاقة المرحلة الدراسية (`stage-card`)
- `border-radius: 30px` (أكبر نصف قطر في الموقع)، أيقونة كبيرة شفافة تتلون
  وتتكبر عند hover، حدود تتحول للون `--primary` مع glow.

### الهيرو (Hero Section)
- عنوان بتدرج نصي: `linear-gradient(to right, #fff, var(--cyan))` مع
  `-webkit-background-clip: text`.
- دائرة ضوئية خلفية ضخمة (`600x600px`) بتدرج primary→accent، `filter: blur(150px)`،
  و`opacity: 0.4`، مع أنيميشن نبض بطيء (`pulse` كل 10 ثواني).
- بطاقة عائمة (`hero-card-floating`) مائلة بزاوية `-5deg` مع أنيميشن `float`
  (حركة صعود ونزول لطيفة كل 6 ثواني)، خلفية زجاجية بـ `backdrop-filter: blur(20px)`.
- خط فاصل عمودي على يمين الوصف (`border-right: 3px solid var(--accent)`).

### شريط التقدم (Loading Bar)
- خلفية رمادية داكنة `#334155`، الشريط الملوّن بلون `--accent`، `border-radius: 10px`.

### الهيدر (Sticky Header)
```css
.header {
    position: sticky;
    background: rgba(15, 23, 42, 0.9);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--border-glass);
}
```
- روابط التنقل فيها خط سفلي متحرك (`::after`) يتمدد من العرض 0 إلى 100% عند hover
  بلون `--cyan`.

---

## 6. الحركة والانتقالات (Motion)

قواعد ثابتة في كل الموقع:
- انتقالات عامة: `transition: 0.3s` أو `0.4s` (لا يوجد أسرع من كده تقريبًا).
- Hover على البطاقات: `translateY(-8px)` إلى `translateY(-10px)`.
- Hover على الأزرار: `translateY(-2px)` + زيادة قوة الـ glow shadow.
- أنيميشن `pulse` (نبضة ضوئية خلفية الهيرو): 10 ثواني، `infinite alternate`.
- أنيميشن `float` (بطاقة الهيرو العائمة): 6 ثواني، `ease-in-out infinite`.

---

## 7. الشكل الهندسي (Border Radius Scale)

استخدم نفس التدرج ده بالظبط عبر المشروع:

| العنصر | القيمة |
|---|---|
| أزرار (pills) | `50px` (دائري كامل) |
| بطاقات كبيرة (stage-card, teacher-cta) | `30px` |
| بطاقات متوسطة (feature-box, course-card, subject-pill) | `15px – 20px` |
| قوائم منسدلة (profile-dropdown) | `15px` |
| تاجات صغيرة (tag) | `20px` |

---

## 8. هيكلة الصفحة (Sections)

1. **Header ثابت (Sticky + Blur)** — شعار (بلون accent جزئي) + روابط + أزرار
   دخول/بروفايل، مع قائمة موبايل جانبية (`mobile-menu`) تنزلق من اليمين.
2. **Hero** — عنوان بتدرج نصي + وصف بخط فاصل + زرين CTA + بطاقة عائمة بها
   صورة/إحصائية + شريط تقدم.
3. **Stats bar** — شريط أفقي بخلفية تدرج شفاف خفيف (`rgba(primary)` +
   `rgba(accent)`) يعرض أرقام كبيرة (`stat-num`) مع تسمية بلون accent.
4. **Features grid** — بطاقات زجاجية 3 أعمدة، كل بطاقة أيقونة متدرجة اللون
   + عنوان + وصف، مع خط علوي متحرك عند hover.
5. **Subjects grid** — شبكة "حبوب" (pills) للمواد الدراسية، تتحول للون
   `--primary` بالكامل عند hover.
6. **Courses grid** — بطاقات كورس داكنة بصورة، اسم مدرّس بلون accent، سعر،
   meta info بخط فاصل سفلي.
7. **Teachers grid** — بطاقات مدرّسين بصورة دائرية محاطة بإطار primary.
8. **Stages grid** — 4 بطاقات مراحل دراسية كبيرة بأيقونة مركزية ضخمة شفافة.
9. **Teacher CTA** — بانر كبير بخلفية تدرج داكن (`bg-card` → أسود) وأيقونة
   عملاقة شفافة في الخلفية.
10. **Footer** — 4 أعمدة (`2fr 1fr 1fr 1fr`)، عناوين أعمدة بلون accent، روابط
    بلون `text-sec`، سوشيال ميديا، حقوق نشر بخط فاصل علوي.

---

## 9. الاستجابة (Responsive Rules) — من الكود

- `max-width: 900px`: الهيرو يتحول لعمود واحد، تختفي البطاقة العائمة، فوتر
  يتحول لعمودين (قسم "عن المنصة" يمتد على عمودين).
- `max-width: 768px`: تختفي روابط الهيدر الرئيسية ويظهر زر القائمة الجانبية
  (`nav-toggle`)، تختفي قائمة البروفايل العادية ويظهر نسخة موبايل منها.
- `max-width: 576px`: شبكة المواد الدراسية تتحول لعمودين فقط بدل التمدد التلقائي.

---

## 10. قواعد تقنية عامة للبناء

- استخدم **CSS Custom Properties** (زي `:root` أعلاه) من أول سطر — كل الألوان
  تُستدعى بمتغيرات، ممنوع كتابة hex codes مباشرة داخل الكومبوننتس.
- استخدم **CSS logical properties** (`padding-inline-start` بدل
  `padding-right`) لضمان التوافق مع RTL بشكل سليم حتى لو غيّرت الاتجاه لاحقًا.
- `box-sizing: border-box` على كل العناصر (موجودة في الكود الأصلي).
- استخدم `backdrop-filter: blur()` بحذر (أداء أقل على الموبايلات الضعيفة) —
  فعّله فقط على الهيدر والبطاقات العائمة، مش على كل البطاقات.
- كل تأثيرات الـ glow (`box-shadow` بألوان شفافة) لازم تستخدم نفس ألوان
  `--primary` أو `--accent` بشفافية بين `0.2` و`0.6` فقط، للحفاظ على الاتساق.

---

## 11. الصفحات والوظائف المطلوبة (Pages & Features)

المشروع مش لاندينج بيدج تسويقية بس — فيه 3 أجزاء منفصلة بصلاحيات مختلفة.

### 11.1 الصفحة الرئيسية (Landing Page — عامة لأي زائر)
- تتبع هيكلة الأقسام في قسم 8 بالكامل (Header, Hero, Stats, Features,
  Subjects, Courses, Teachers, Stages, CTA, Footer).
- زر واحد بس في الهيدر مكان "دخول/حساب جديد": **"دخول الطالب"** يوجّه
  لصفحة `/student-login`.
- **ممنوع** يظهر أي رابط أو إشارة للوحة تحكم المشرف في أي مكان بالصفحة —
  لا في الهيدر، لا في الفوتر، لا في أي زرار.

### 11.2 لوحة تحكم المشرف (Admin Panel — مخفية تمامًا)
- **الرابط**: مسار غير متوقع وغير مرتبط بأي زرار في الموقع كله، مثال:
  `/sa-control-x7k9` (غيّره لمسار عشوائي خاص بيك، ومتستخدمش `/admin` لأنه
  أول حاجة أي حد هيجربها).
- صفحة تسجيل دخول منفصلة قبل الدخول للوحة: يوزر + باسورد واحد بس، مخزّن
  كحساب "admin" في Firebase Authentication.
- بعد الدخول الناجح، اللوحة فيها:
  - **فورم إضافة طالب جديد**: الاسم الكامل، يوزر تسجيل الدخول، باسورد،
    مواعيد الحصص (يوم + وقت لكل حصة، إمكانية إضافة أكتر من موعد)، لينك كل حصة.
  - **جدول/قائمة بكل الطلاب** المسجلين، مع أزرار تعديل وحذف لكل طالب على حدة.
- التصميم يتبع نفس نظام الألوان والمكونات (Dark Glass)، لكن بتخطيط إداري
  (Sidebar + جدول بيانات) مش هيرو تسويقي — البساطة والوضوح أهم من التأثيرات
  البصرية هنا.

### 11.3 صفحة دخول الطالب (Student Login)
- **الرابط**: `/student-login` — فورم بسيط: يوزر + باسورد بس.
- **مفيش** رابط "إنشاء حساب جديد" في أي مكان في الصفحة دي — التسجيل كله
  يدوي من المشرف.
- بعد الدخول الناجح، يوديه لصفحته الخاصة (`/dashboard` أو `/student/[id]`)
  وتعرض:
  - اسم الطالب.
  - جدول مواعيد حصصه (يوم + وقت لكل حصة).
  - زرار "انضم للحصة" بجانب كل موعد يفتح لينك الحصة مباشرة.

### 11.4 هيكل بيانات Firebase المقترح

```
Firestore:
/students/{studentId}
    - fullName: string
    - username: string
    - sessions: array of { day, time, link }
    - createdAt: timestamp

/admins/{adminId}
    - username: string
    (يُستخدم للتحقق إن الحساب المسجّل دخوله فعلاً مشرف، مش طالب عادي)

Firebase Authentication:
    - كل طالب: حساب (email أو username محوّل لصيغة إيميل داخليًا + password)
    - حساب المشرف: حساب منفصل تمامًا، اتحقق من صلاحيته عبر وجود مستنده في
      /admins قبل ما تسمحله يشوف لوحة التحكم.
```

⚠️ **قاعدة أمان مهمة**: أي طلب وصول للوحة المشرف لازم يتأكد أولاً إن الـ UID
بتاع الحساب المسجّل دخوله موجود في `/admins` — مش مجرد إنه سجّل دخول بنجاح.
لو مفيش الفحص ده، أي طالب عادي ممكن يجرب يدخل على رابط اللوحة ويشوف بيانات
كل الطلاب.

---

## 12. الوضع الفاتح (Light/White Theme Toggle)

> **القاعدة الأساسية**: الدارك ثيم (Dark Glass) هو **الوضع الافتراضي**
> اللي الموقع بيفتح بيه لأي زائر جديد. الوضع الفاتح خاصية إضافية اختيارية،
> مش العكس.

### آلية التبديل
- زرار toggle (أيقونة شمس/قمر ☀️/🌙) مكانه في الهيدر، يضيف/يشيل
  `data-theme="light"` على عنصر `<html>`.
- استخدم `localStorage` لحفظ اختيار الزائر (`theme: 'light'` أو ما يحفظش
  حاجة = دارك افتراضي) عشان يفضل معاه لما يرجع تاني.
- كل الألوان في المشروع لازم تكون متغيرات CSS (زي قسم 2) — التبديل هيبقى
  مجرد تعريف نفس أسماء المتغيرات بقيم مختلفة جوا `[data-theme="light"]`،
  بدون أي تغيير في الـ HTML أو أسماء الكلاسات.

### قيم الوضع الفاتح (مبنية على نفس ألوان شعارك، مش ألوان عشوائية)

```css
[data-theme="light"] {
    --bg-core: #f4f7f9;        /* خلفية فاتحة رمادية-زرقاء ناعمة، مش أبيض خالص */
    --bg-card: #ffffff;        /* بطاقات بيضاء نقية */
    --primary: #0a425b;        /* نفس أزرق الشعار بالظبط - شغال ممتاز فوق فاتح */
    --accent: #2f5f75;         /* نفس لون الشعار الثانوي */
    --cyan: #1786ab;           /* سماوي أغمق شوية من نسخة الدارك عشان يبان واضح */
    --text-main: #0f172a;      /* نص أساسي غامق (معكوس عن الدارك) */
    --text-sec: #475569;       /* نص ثانوي رمادي غامق */
    --glass: rgba(10, 66, 91, 0.04);        /* زجاجية خفيفة بلون العلامة */
    --border-glass: rgba(10, 66, 91, 0.12); /* حدود بلون العلامة الشفاف */
}
```

**تعديلات إضافية لازمة في الوضع الفاتح (مش مجرد استبدال متغيرات)**:
- دائرة الـ glow الخلفية في الهيرو (`hero::before`) وكل الـ `box-shadow`
  الخاصة بالـ glow: قلّل الـ `opacity` (من `0.4` لحوالي `0.15`) عشان متبقاش
  تقيلة وغير مريحة فوق خلفية بيضاء.
- خلفية الهيدر الشفافة (`rgba(15, 23, 42, 0.9)` في الدارك) تتحول لنسخة
  فاتحة زي `rgba(244, 247, 249, 0.9)` جوا `[data-theme="light"]`.
- التدرج النصي في عنوان الهيرو (`linear-gradient(to right, #fff, var(--cyan))`)
  محتاج نسخة تانية في الوضع الفاتح، مثلاً
  `linear-gradient(to right, var(--primary), var(--cyan))`، لأن الأبيض
  `#fff` مش هيبان على خلفية فاتحة.

---

## 13. تقنية الويب التقدمي (PWA — Progressive Web App)

الموقع لازم يشتغل كـ **PWA كامل**، يعني الزائر يقدر "يثبّت" الموقع على
موبايله أو جهازه زي أي تطبيق عادي، مع دعم أساسي للعمل حتى لو النت ضعيف.

### 13.1 ملف الـ Manifest (إلزامي)
لازم ملف `manifest.json` في روت المشروع، متضمن فيه من `<head>` بـ:
```html
<link rel="manifest" href="/manifest.json">
```
محتوى الملف لازم يشمل:
```json
{
  "name": "Smart Academy",
  "short_name": "Smart Academy",
  "description": "منصة سمارت أكاديمي التعليمية",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#071a26",
  "theme_color": "#0a425b",
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```
- `background_color` و`theme_color` لازم تتساوى مع `--bg-core` و`--primary`
  المتغيرات المعتمدة في قسم 2 (الدارك ثيم) — مش ألوان عشوائية.
- الأيقونات (`icons`) تتولد من شعار Smart Academy نفسه (اللوجو اللي حللناه
  في القسم الخاص بالألوان)، بمقاسات 192x192 و512x512 بكسل كحد أدنى.

### 13.2 Service Worker (إلزامي)
- ملف `service-worker.js` في روت المشروع، مسجّل من الـ JavaScript الرئيسي:
```javascript
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.register('/service-worker.js');
}
```
- الوظيفة الأساسية المطلوبة: **تخزين مؤقت (caching)** للملفات الثابتة
  (HTML, CSS, JS, الشعار، الخطوط) عشان اللاندينج بيدج تفتح حتى لو الاتصال
  ضعيف أو منقطع لحظيًا.
- **لا يشمل** تخزين بيانات الطلاب أو الحصص أوفلاين (دي بيانات حساسة وحية من
  Firebase، ومينفعش تتخزن Cache عادي على الجهاز).

### 13.3 قواعد HTTPS والتفعيل
- PWA **محتاج HTTPS إجباريًا** عشان الـ Service Worker يشتغل — أي استضافة
  حديثة (Vercel, Netlify, Firebase Hosting) بتوفره تلقائيًا ومجانًا.
- لازم Meta tags إضافية في `<head>` للتوافق مع الموبايل:
```html
<meta name="theme-color" content="#0a425b">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
```

### 13.4 نطاق تطبيق PWA على الصفحات التلاتة
- **اللاندينج بيدج**: PWA كامل بكل مميزاته (تثبيت، تخزين مؤقت للملفات الثابتة).
- **صفحة دخول الطالب وصفحته الخاصة**: جزء من نفس الـ PWA (نفس manifest)،
  لكن بيانات الجدول والحصص نفسها لازم تتحدّث دايمًا (`network-first`)، مش
  تتقرأ من الكاش، عشان الطالب دايمًا يشوف آخر تحديث من المشرف.
- **لوحة تحكم المشرف**: **متستبعدش من الـ Service Worker** — أضف استثناء في
  الكود يمنع تخزين أي صفحة تحت مسار اللوحة المخفية في الكاش، عشان مفيش أي
  احتمال إن بيانات إدارية حساسة تتخزن محليًا على جهاز حد.

---

## 14. تجنب "شكل الموقع المصمم بالـ AI" (Anti-Generic Patterns)

فيه أنماط بصرية وكتابية بقت "بصمة" واضحة على أي موقع اتبنى بأداة AI من غير
تدخل بشري حقيقي — الهدف هنا إن الموقع يحس الزائر إنه اتصمم بعناية لعلامة
تجارية حقيقية (Smart Academy)، مش قالب عام اتولّد أوتوماتيك. القواعد دي
**تُقيّد** بعض التفاصيل اللي اتحطت في الأقسام اللي فاتت، مش تلغيها بالكامل.

### 14.1 الألوان والتأثيرات البصرية
- **خفّف من الـ "glow blobs"**: الدائرة الضوئية الضخمة خلف الهيرو (قسم 5) —
  خليها موجودة لكن **بحجم أصغر أو شفافية أقل** من اللي اتكتب، ومتخليهاش
  العنصر البصري الوحيد اللي بيلفت النظر. الأسلوب ده (خلفية داكنة + دائرة
  نيون شفافة) بقى نمط متكرر جدًا في مواقع الـ AI، فخليه تفصيلة مش البطل.
- **متكررش نفس تأثير الـ hover** (`translateY` + `box-shadow` أقوى) على
  **كل** عنصر في الصفحة بدون استثناء — اختار 2-3 أماكن بس يستاهلوا الحركة
  دي (زرار CTA الرئيسي، بطاقة الكورس)، وخلي باقي العناصر تفاعلها أبسط
  (تغيير لون بسيط، مثلاً) عشان الحركة متبقاش "ضجة" متكررة في كل حتة.
- **التدرجات النصية (gradient text)**: استخدمها في العنوان الرئيسي بس
  (قسم 5)، ومتكررهاش في عناوين فرعية كتير — التكرار ده من أوضح علامات
  التصميم الأوتوماتيكي.

### 14.2 التخطيط (Layout)
- **متخليش كل قسم شبكة بطاقات 3 أعمدة متطابقة**. لو كل الأقسام (Features,
  Subjects, Courses, Teachers, Stages) نفس الشكل بالظبط (كارت بحدود مستديرة
  + أيقونة فوق + عنوان + وصف)، ده أوضح نمط "قالب AI". نوّع:
  - قسم يكون شبكة بطاقات عادية.
  - قسم تاني يكون سلايدر أفقي (Carousel) بدل الشبكة.
  - قسم تالت يكون تخطيط غير متماثل (عنصر كبير + عناصر صغيرة حواليه) بدل
    شبكة منتظمة تمامًا.
- **الأرقام التسلسلية (01 / 02 / 03)**: متستخدمهاش كديكور لو المحتوى مش
  فعلاً خطوات متتالية بترتيب مهم. لو استخدمتها، تكون لخطوات حقيقية بس
  (مثلاً: "إزاي تبدأ" 1-سجّل 2-اختار مدرّس 3-احجز الحصة).

### 14.3 الكتابة والمحتوى (Copywriting)
- **تجنّب الجمل التسويقية العامة اللي بتتكرر في أي موقع AI** زي: "في عالم
  يتغير باستمرار..."، "نطلق لك العنان لتحقيق أحلامك"، "ثورة في عالم
  التعليم"، "تجربة تعليمية لا مثيل لها". الكلام ده عام أوي ومش مرتبط بحاجة
  حقيقية في Smart Academy تحديدًا.
- **اكتب بمصطلحات حقيقية وملموسة** بدل الحماس العام: بدل "رحلة تعليمية
  استثنائية" اكتب حاجة زي "حصص مباشرة مع مدرّسك، في معادك، من غير ما
  تسافر". التفاصيل الملموسة (وقت، مدرّس، حصة) أقوى من الكلام الحماسي الفاضي.
- **الأزرار والعناوين تتكلم بصيغة الفعل الواضح**: "احجز حصتك" مش "ابدأ
  رحلتك"، "شوف جدولك" مش "اكتشف عالمك التعليمي".

### 14.4 الأيقونات والإيموجي
- **قلّل استخدام الإيموجي في العناوين والأقسام** (📱 🎓 ⭐ 👨‍🎓ـ إلخ). إيموجي
  واحد أو اتنين بالكتير في الموقع كله (مثلاً في زرار "انضم للحصة" 🔗)،
  مش في كل عنوان قسم — الاستخدام المكثف للإيموجي في العناوين بقى من أوضح
  علامات "الموقع ده مبني بـ AI بسرعة".
- استخدم أيقونات Font Awesome (قسم 4) **بس في الأماكن اللي فعلاً محتاجة
  توضيح بصري وظيفي** (زرار تسجيل خروج، حالة نجاح/خطأ)، مش كديكور إضافي على
  كل عنوان أو كارت.

### 14.5 القاعدة العامة
قبل ما توافق على أي قسم بنى Claude Code، اسأل نفسك: **"لو شفت الشكل ده في
10 مواقع تانية بنفس الترتيب بالظبط، هل هو مميز فعلاً ولا نمط عام؟"** — لو
الإجابة "نمط عام"، اطلب تعديل بسيط يخليه مرتبط بهوية Smart Academy تحديدًا
(الألوان من الشعار، لغة المحتوى، ترتيب مختلف شوية عن المتوقع).

---

## ملاحظة أخيرة

الملف ده تحليل لنظام تصميم (Design System) مبني على **قراءة الكود كمرجع
تقني** — القيم (الألوان، الخطوط، الانتقالات) دي "لغة تصميم" عامة زي ما بتستخدمها
آلاف المواقع (Glassmorphism + Neon على خلفية داكنة هو ستايل شائع جدًا حاليًا في
EdTech وSaaS). البناء الفعلي (الـ HTML structure، أسماء الكلاسات، المحتوى)
هيتكتب من الصفر ويخصك انت بالكامل، مش نسخ حرفي.