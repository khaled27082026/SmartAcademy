# CLAUDE.md — دليل مشروع سمارت أكاديمي الشامل

> المرجع التوجيهي الرئيسي لأي جلسة عمل قادمة على هذا المشروع. مبني بالكامل
> على القرارات الفعلية اللي اتاخدت والكود الحقيقي الموجود — مش افتراضات.
> لو أي تعديل مستقبلي هيغيّر قاعدة من القواعد دي، حدّث الملف ده في نفس
> الـ commit.

---

## 1. نظرة عامة على المشروع (Project Overview)

**إيه هو:** منصة تعليمية عربية (RTL بالكامل) بتربط الطالب بمعلم خاص عبر
حصص أونلاين مباشرة مجدولة أسبوعيًا، بمتابعة من مشرف تعليمي وإدارة مالية
شاملة من المدير.

**الأدوار الأربعة** (كل واحد بلوحة مستقلة تمامًا، صفر تداخل):

| الدور | يعمل إيه |
|---|---|
| **الطالب** | يشوف جدوله، يدخل حصته من رابط جاهز، يستقبل تذكير قبلها بـ10 دقايق |
| **المعلم** | يدير جدوله، يسجّل حضور وتقييم كل حصة، يتابع مستحقاته المالية |
| **المشرف** | يسجّل طلاب/معلمين جداد، يدير جدولهم الأسبوعي، يتابع التقارير |
| **المدير** | البوابة المالية الشاملة للمنصة كلها — مستحقات، اشتراكات، معاملات |

**أبرز التقنيات:** Supabase (Postgres + Auth + Edge Functions + Cron) كخلفية
كاملة، وواجهة أمامية HTML/CSS/JS خام بدون أي Framework أو خطوة Build —
موقع ثابت (Static) بالكامل، جاهز للنشر المباشر على Vercel.

**الهدف من المشروع:** حل مشكلة "الدروس الخصوصية غير المنظّمة" بجدول ثابت،
رابط حصة تلقائي، تذكير Push حقيقي، وتوثيق كامل للحضور والتقييم — بدون أي
تعقيد تقني زايد عن حجم المشروع الفعلي.

---

## 2. التقنيات والمكتبات المعتمدة (Tech Stack & Dependencies)

| الطبقة | المعتمد | لأ |
|---|---|---|
| الواجهة | HTML5 + CSS3 + Vanilla JS (ES6+) | ❌ React / Vue / أي Framework |
| البناء | لا يوجد (Zero Build) | ❌ Webpack / Vite / أي Bundler |
| Layout Utilities | Bootstrap **5.1.0** (RTL build، CDN) — Grid/Utilities بس | ❌ ألوان/مكونات Bootstrap الجاهزة |
| الأيقونات | Font Awesome **6.5.1** (CDN) | ⚠️ لازم تتأكد إن اسم أي أيقونة موجود فعليًا في 6.5.1 قبل استخدامه |
| الخط | Tajawal (Google Fonts، أوزان 300/400/500/700/900) | ❌ أي خط تاني في أي مكان |
| قاعدة البيانات | Supabase (Postgres) عبر `@supabase/supabase-js@2` (CDN في الواجهة) | ❌ أي ORM أو ملفات import/require في كود الواجهة |
| الخلفية الحيّة | Supabase Edge Functions (Deno) | — |
| الجدولة | `pg_cron` + `pg_net` | — |
| الإشعارات | Web Push API + `web-push@3` (داخل الـEdge Function فقط) | ❌ أي مكتبة Push تانية |
| PWA | Service Worker يدوي (Cache API مباشرة) | ❌ Workbox أو أي مولّد SW |
| الاستضافة | Vercel (موقع ثابت) | — |

**ملاحظة عن `package.json`:** موجود بس لدعم سكريبت ترحيل محلي مرة واحدة
(`supabase/migrate-managers-to-auth.mjs`) — **مش جزء من الموقع المنشور
خالص**. الموقع نفسه بيستدعي كل حاجة عبر `<script src="https://cdn...">`
مباشرة. `vercel.json` بيوضّح صراحة إنه مفيش Build مطلوب.

---

## 3. قواعد هندسة البرمجيات والمعمارية (Architecture & Coding Style)

### النمط المعماري: Multi-Page App + Server-Trusted RPC

- كل دور/شاشة = **صفحة HTML مستقلة كاملة** (مش SPA بتوجيه JS). كل صفحة
  فيها كتلة `<script>` واحدة بمنطقها، Vanilla JS، بدون Components حقيقية.
- **الواجهة الأمامية لا تلمس أي جدول في Supabase مباشرة أبدًا.** كل قراءة
  أو كتابة تمر عبر دالة RPC واحدة محدَّدة الغرض (`SECURITY DEFINER`
  functions). منطق العمل (Business Logic) يعيش **جوه قاعدة البيانات**،
  مش في الواجهة.
- كل دالة RPC جديدة لازم: (أ) اسم واضح بالإنجليزية بصيغة `verb_noun`
  (`get_student_sessions`, `set_teacher_sessions`, `verify_student_login`)،
  (ب) `security definer` + `set search_path = public`، (ج) فحص صلاحية
  داخلي (حتى لو بسيط) قبل أي قراءة/كتابة، (د) `grant execute` صريح لـ
  `anon` في `policies.sql` — بدونه هتفشل الدالة بصمت من الواجهة.

### تنظيم الملفات

- كل صفحة HTML **self-contained**: `<style>` محلي للتنسيقات الخاصة بيها
  فوق كتلة `css/style.css` المشتركة، و`<script>` محلي لمنطقها.
- الأكواد المشتركة بين أكتر من صفحة تُستخرج لملف في `js/` واحد بس (مش
  تتكرر) — أمثلة: `app.js` (جلسات/توقيت/PWA)، `route-guard.js` (حماية)،
  `push-notifications.js` (الإشعارات).
- ملفات SQL منظمة حسب الغرض مش حسب الترتيب الزمني: `main-schema.sql`
  (جداول)، `functions.sql` (كل الـRPCs)، `policies.sql` (RLS + صلاحيات).
  ملفات الترحيل التاريخية (اللي خلصت مهمتها) تتنقل لـ`supabase/archive/`
  بدل ما تتمسح — مرجع تاريخي مفيد.

### أسلوب تسمية

- **دوال JS مشتركة:** بادئة `sa` (اختصار Smart Academy) — `saGetSession`,
  `saSetSession`, `saFormatDubai`, `saEscapeHtml`. أي دالة عامة تُستخدم
  في أكتر من مكان بتاخد البادئة دي حتى لو معرّفة محليًا داخل صفحة.
- **دوال RPC:** إنجليزي كامل، `snake_case`، فعل صريح في الأول
  (`get_`, `set_`, `list_`, `create_`, `update_`, `delete_`, `verify_`,
  `sync_`).
- **كلاسات CSS:** `kebab-case` وصفي بادئ باسم المكوّن
  (`.subject-group-icon`, `.pricing-trial-cta`, `.notif-bell-panel`) —
  بدون أي منهجية BEM صارمة، بس التسلسل الهرمي واضح من الاسم نفسه.
- **مفاتيح `localStorage`:** بادئة `sa_` دايمًا (`sa_current_student`,
  `sa_students`) — تسهّل حصر كل حاجة بيستخدمها المشروع في `localStorage`.

### معالجة الأخطاء

- كل نداء `await sb.rpc(...)` لازم يكون جوه `try/catch`، مع
  `console.error('<رسالة عربية واضحة>:', e)` — الرسالة بتوصف الفعل اللي
  فشل، مش رسالة عامة زي "حدث خطأ".
- رسائل الخطأ اللي بتظهر للمستخدم **فصحى دايمًا**، وبتوصف المشكلة
  ومحاولة الحل ("تعذّر تحميل البيانات، حاول مرة أخرى") — بدون اعتذار
  زايد أو غموض.
- فشل شبكة/RPC لا يوقف الصفحة كلها — العنصر المتأثر بس بيعرض حالة فاضية
  أو رسالة خطأ محلية (`dash-empty`), والباقي يفضل شغال.

---

## 4. هيكلة الملفات والمجلدات

```
SmartAcademy/
├── index.html                      # اللاندينج بيدج العامة
├── admin-login.html                # دخول موحّد: مدير / مشرف / معلم
├── manager-dashboard.html          # لوحة المدير — البوابة المالية
├── supervisor-dashboard.html       # الصفحة الرئيسية للمشرف
├── supervisor-students.html        # إدارة الطلاب + جدولهم
├── supervisor-teachers.html        # إدارة المعلمين + جدولهم
├── teacher-dashboard.html          # لوحة المعلم
├── student-dashboard.html          # لوحة الطالب
├── student-login/index.html        # دخول الطالب (مسار مستقل)
├── manifest.json                   # PWA manifest
├── service-worker.js               # تخزين مؤقت + Push + فتح رابط الإشعار
├── vercel.json                     # تعطيل Build (موقع ثابت)
├── .gitignore
├── css/style.css                   # نظام التصميم الكامل (Design Tokens)
├── js/
│   ├── app.js                      # توقيت دبي، الجلسات، PWA، الثيم
│   ├── push-notifications.js       # اشتراك Push + جرس الإشعارات
│   ├── route-guard.js              # حماية الصفحات
│   ├── supabase-config.js          # SUPABASE_URL / SUPABASE_ANON_KEY
│   └── supabaseClient.js           # window.saSupabase
├── assets/images/                  # الشعار، رسوم، أيقونات PWA
└── supabase/
    ├── main-schema.sql             # ⚠️ يمسح البيانات — تشغيل مرة واحدة فقط
    ├── functions.sql               # كل دوال RPC — آمن التكرار دائمًا
    ├── policies.sql                # RLS + صلاحيات — آمن التكرار دائمًا
    ├── schedule-reminder-cron.sql  # جدولة pg_cron للتذكير
    ├── functions/notify-upcoming-classes/index.ts   # Edge Function
    └── archive/                    # ملفات SQL تاريخية — مرجع فقط
```

**ترتيب تشغيل SQL إلزامي:** `main-schema.sql` ← `functions.sql` ←
`policies.sql`. الأول لا يُشغَّل إلا مرة واحدة على قاعدة فاضية (بيمسح
الجداول قبل ما يعيد إنشائها). الاتنين التانيين آمنين للتشغيل في أي وقت.

---

## 5. إدارة الحالة وتدفق البيانات (State & Data Flow Rules)

**لا يوجد State Management framework — الحالة موزّعة على ثلاث طبقات
واضحة الحدود، وممنوع الخلط بينها:**

1. **الجلسة (Session)** — `localStorage` عبر `saSetSession`/`saGetSession`
   بس. صلاحية 30 يوم. مفتاح منفصل لكل دور. **بيانات الهوية فقط**
   (id, phone, fullName) — مش مصدر حقيقة لأي بيانات تتغيّر (جدول، حضور،
   تقييم).
2. **الحقيقة الدائمة (Source of Truth)** — Supabase حصرًا. أي بيانات
   بتتغيّر (الجدول، الحضور، التقييم، الإشعارات) **تُجلب Fresh من الخادم
   عند كل تحميل صفحة** عبر RPC. **ممنوع تمامًا** الاعتماد على نسخة محلية
   قديمة مخزّنة كـ"كاش" لإعادة رسم أي جزء حي من الواجهة — ده بالظبط السبب
   اللي كسّر كارت "حصة اليوم" في لوحة المعلم قبل كده (راجع قسم التحديات
   في التقرير المعماري).
3. **حالة الواجهة اللحظية** — متغيرات JS عادية (`let`/closures) داخل كل
   صفحة، بتتصفّر مع أي Reload.

**قاعدة صارمة:** أي دالة بتعيد رسم عنصر حي بعد إجراء المستخدم (تسجيل
حضور، تقييم، حفظ جدول) لازم تجيب البيانات **من Supabase تاني**، مش من
أي متغيّر محلي قديم — حتى لو "شكله هيرجع نفس القيم غالبًا".

**نموذج المصادقة لكل دور** (مهم عشان يوجّه أي قرار تعديل مستقبلي):

| الدور | التحقق | جلسة موثّقة حقيقية بعد الدخول؟ |
|---|---|---|
| مدير | Supabase Auth حقيقي (`auth.uid()`) | ✅ نعم |
| مشرف/معلم/طالب | دالة RPC (هاتف + كلمة مرور)، مرة واحدة | ❌ لا — كل RPC بعد كده يصدّق المعرّف من العميل مباشرة |

هذا الفرق مقصود ومُوثَّق (مش سهو)، لكنه **فجوة أمنية معروفة** — راجع
القسم اللي بعده قبل أي تعديل بيلمس بيانات حسّاسة لدور غير المدير.

---

## 6. الأمان والتحقق من المدخلات (Security & Validation)

### القاعدة الذهبية
**لا Policy مباشرة على أي جدول أبدًا.** كل جدول في Supabase عليه RLS
مفعّل (`enable row level security`) **بدون** أي `create policy` — رفض
كامل بالـDefault لأي وصول عبر REST مباشر. الوصول الوحيد المسموح: دوال
RPC بصلاحية `security definer`، بعد `grant execute` صريح لـ`anon` في
`policies.sql`.

### الفجوة المعروفة — لازم تُقرأ قبل أي تعديل على المصادقة
المشرف/المعلم/الطالب بلا جلسة Supabase Auth حقيقية (راجع قسم 5). يعني:
كل دالة RPC بتاخد معرّف (هاتف أو UUID) من العميل **وتصدّقه على طول**، من
غير ما تتحقق إن المتصل هو صاحب الـID فعلًا. **قبل أي إطلاق واسع النطاق،
لازم ترقية الأدوار التلاتة لنفس نموذج المدير (Supabase Auth حقيقي).**
لحد ما يحصل ده، تعامل مع أي RPC جديد بتاخد `p_recipient_id`/`p_user_id`
كأنها بتُستدعى من أي حد معاه مفتاح anon العام — لا تخزّن فيها حاجة أخطر
من اللي موجود بالفعل.

### قواعد تحقق ملزمة لأي كود جديد

- **أي نص حر (اسم، عنوان، محتوى) بيتحقن في `innerHTML`** لازم يمر على
  `saEscapeHtml()` (موجودة في `js/push-notifications.js`) قبل الحقن —
  بدون استثناء، حتى لو المصدر "موثوق" (المشرف نفسه ممكن يدخل نص فيه وسم
  بالغلط).
- **أسرار حقيقية** (VAPID private key، service role key) **ممنوعة تمامًا**
  من كود الواجهة — تعيش فقط كـSecrets داخل الـEdge Function. المفتاح
  العام (VAPID public / anon key) آمن يتكتب في الكود لأنه مُصمَّم يكون
  علني.
- **أي جدول جديد** لازم RLS مفعّل من أول لحظة إنشاءه، حتى لو مفيش وقت
  كافٍ لكتابة كل الـRPCs بتاعته فورًا — أفضل من نسيان تفعيله بعدين.

---

## 7. نظام التصميم البصري (Design System)

> ملخص مكثّف — التفاصيل الكاملة (الألوان، الخطوط، المكونات، القواعد
> التقنية، الوضع الفاتح، PWA، تجنّب "شكل الموقع المصمَّم بالـAI") موجودة
> في نسخة موسّعة من هذا الملف تاريخيًا، وكل القيم دي **مطبَّقة فعليًا**
> في `css/style.css` — راجعه دايمًا كمصدر الحقيقة، مش الوصف هنا.

**الهوية:** Dark Glassmorphism + Neon Gradients، خلفية داكنة جدًا
(`--bg-core: #071a26`)، بطاقات زجاجية شبه شفافة، تدرجات لونية بين الأزرق
البحري (`--primary: #0a425b`) والتركواز (`--cyan: #4fb3d9`) — الألوان
مشتقة من شعار المنصة نفسه، مش عشوائية.

**قواعد ثابتة ممنوع كسرها:**

- **صفر hex codes مباشرة داخل المكونات** — كل لون عبر CSS Custom
  Property من `:root`.
- **خط Tajawal فقط** على كل عنصر في الموقع بلا استثناء.
- **مقياس نصف قطر واحد:** `50px` (أزرار) / `30px` (كروت كبيرة) /
  `20px` (متوسطة) / `15px` (صغيرة) — لا قيم عشوائية تانية.
- **سرعتين انتقال بس:** `0.3s` (سريع) و`0.4s` (بطيء) — لا سرعات زايدة.
- **RTL بالكامل** — استخدم CSS logical properties
  (`padding-inline-start` لا `padding-right`) دايمًا.

**قاعدة اللغة (صارمة، اتفرضت بعد مشكلة حقيقية):**

- **فصحى كاملة** في كل لوحات النظام (طالب، معلم، مشرف، مدير) — بلا أي
  عامية مصرية أو خليجية، بلا استثناء، حتى في التعليقات البرمجية ونصوص
  الإشعارات الفعلية.
- **لمسة خليجية/إماراتية خفيفة مسموح بيها في اللاندينج بيدج فقط**
  (`index.html`) — كلمات زي "وايد"، "زين" مقبولة هناك بس، مش في أي مكان
  تاني.
- قبل أي نص جديد، افحص: هل فيه "مش" بدل "ليس"، "علشان" بدل "لكي/حتى"،
  صيغة نفي "ما...ش" (علامة مصرية قوية)، أو "دقايق" بدل "دقائق"؟ لو
  الصفحة مش اللاندينج بيدج، صحّحها فورًا.

**تجنّب "شكل الموقع المصمَّم بالـAI":** خفّف من الـglow blobs، لا تكرر
نفس تأثير الـhover على كل عنصر، نوّع تخطيط الأقسام (مش كل قسم شبكة 3
أعمدة متطابقة)، تجنّب الجمل التسويقية العامة، وقلّل الإيموجي في العناوين.

---

## 8. أوامر التشغيل والتطوير (Build & Dev Commands)

المشروع **بدون** `npm run dev` أو أي سكريبت تشغيل — كل صفحة HTML تُفتح
مباشرة. للاختبار المحلي:

```bash
# تشغيل سيرفر محلي بسيط من جذر المشروع
python3 -m http.server <PORT> --directory /path/to/SmartAcademy

# افتح على: http://localhost:<PORT>/index.html
```

**قبل أي اختبار حي لتعديل CSS/JS/HTML:**

```bash
# 1) ارفع رقم نسخة الكاش في service-worker.js (إلزامي مع كل تعديل)
#    const CACHE_NAME = 'smart-academy-cache-vNNN';

# 2) في المتصفح، ألغِ تسجيل أي Service Worker قديم قبل إعادة التحميل
navigator.serviceWorker.getRegistrations().then(rs => rs.forEach(r => r.unregister()));

# 3) أعد التحميل بـquery string جديد لتفادي أي HTTP cache قديم
#    ?bust=<timestamp>
```

**فحص Edge Function (Deno) قبل النشر:**

```bash
deno check supabase/functions/notify-upcoming-classes/index.ts
deno lint supabase/functions/notify-upcoming-classes/index.ts
```

**نشر Edge Function بعد أي تعديل عليها:**

```bash
npx supabase functions deploy notify-upcoming-classes
```

**فحص Syntax سريع لأي صفحة HTML (بدون متصفح):**

```bash
# يستخرج كتل <script> ويفحصها بمُفسّر Node
node --check <(python3 -c "import re;print('\n;\n'.join(re.findall(r'<script(?![^>]*src=)[^>]*>(.*?)</script>', open('FILE.html').read(), re.S)))")
```

---

## 9. إرشادات سير العمل والتعديلات المستقبلية (Future Workflow Guidelines)

### قبل أي تعديل
1. اقرأ الكود الحالي فعليًا (لا تفترض) — خصوصًا `css/style.css` والدالة
   المرتبطة في `functions.sql` لو التعديل بيلمس بيانات.
2. لو التعديل بيغيّر جدول أو دالة RPC، اكتبه كملف SQL إضافي منفصل
   (`add-*.sql`) بدل تعديل `main-schema.sql` مباشرة — الأخير مخصص
   للتأسيس مرة واحدة بس.

### أثناء التعديل
3. **صفر hex codes** خارج `:root`. **صفر عامية** خارج اللاندينج بيدج.
4. أي RPC جديدة = تعريف في `functions.sql` **+** grant في `policies.sql`
   في نفس التعديل — نسيان الـgrant يعني فشل صامت من الواجهة.
5. أي تعديل على تصميم متجاوب (Responsive) لازم يتفحص برمجيًا على عرض
   320px على الأقل — لا تكتفِ بالفحص البصري وحده؛ استخدم فحص عدد الأسطر
   الحقيقي (`Range.getClientRects().length`) لأي نص المفروض يفضل سطر
   واحد، مش `scrollWidth <= clientWidth` (غير موثوق داخل Flex).

### بعد التعديل — إلزامي قبل ما تقول "خلصت"
6. ارفع نسخة `CACHE_NAME` في `service-worker.js`.
7. اختبر حيًا في متصفح حقيقي (مش وصف نظري) — سجّل دخول فعلي، افحص
   Console للأخطاء، وتأكد من النتيجة ببيانات حقيقية من Supabase مش
   افتراضات.
8. لو التعديل بيمس بيانات مستخدم حقيقي أثناء الاختبار، أنشئ حسابات
   اختبار بأسماء واضحة (`TEST_DELETE_ME_...`) واطلب من صاحب المشروع
   حذفها بعد التأكيد — معندناش دالة `delete_student`/`delete_teacher`
   حاليًا.
9. حدّث هذا الملف (`CLAUDE.md`) لو التعديل غيّر قاعدة معمارية أو أضاف
   نمط جديد يستاهل يتوثّق.

### فلسفة عامة
- **لا تُقدّم التعقيد قبل ما يستاهله الحجم** — المشروع ده اختار عمدًا
  MPA بدون Build بدل أي Framework، وده قرار صحيح لحجمه الحالي. لا تقترح
  الانتقال لـReact/Next.js إلا لو الحجم فعلًا كبر بما يستاهل.
  عايزني أعمل حاجة "أنضف تقنيًا" بس مش لازمة فعليًا؟ اسأل الأول.
